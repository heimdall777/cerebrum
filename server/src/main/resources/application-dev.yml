server:
  port: 8000

spring:
  h2:
    console:
      enabled: true
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      database: H2
      show-sql: true
      properties:
        hibernate.ddl-auto: create-drop